import {NextResponse}from'next/server';
export function middleware(req:Request){const h=new Headers();h.set('X-Frame-Options','DENY');h.set('X-Content-Type-Options','nosniff');h.set('Referrer-Policy','strict-origin-when-cross-origin');h.set('Permissions-Policy','camera=(), microphone=(), geolocation=()');if(req.method!=='GET'&&req.method!=='HEAD'&&req.method!=='OPTIONS'){const requestOrigin=new URL(req.url).origin;const origin=req.headers.get('origin');const referer=req.headers.get('referer');let sameOrigin=true;if(origin)sameOrigin=origin===requestOrigin;else if(referer){try{sameOrigin=new URL(referer).origin===requestOrigin}catch{sameOrigin=false}}else sameOrigin=false;if(!sameOrigin)return new NextResponse('Forbidden',{status:403,headers:h})}const res=NextResponse.next({request:{headers:req.headers}});h.forEach((v,k)=>res.headers.set(k,v));return res}
export const config={matcher:['/((?!_next/static|_next/image|favicon.ico).*)']};
