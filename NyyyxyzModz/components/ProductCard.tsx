import Link from 'next/link'; import CartButton from './CartButton'; import WishlistButton from './WishlistButton'; import {Product} from '@/lib/catalog'; import {rupiah} from '@/lib/utils';
export default function ProductCard({p}:{p:Product}){const min=Math.min(...p.variants.map(v=>v.price));return <article className="card"><div className="thumb">{p.image?<img src={p.image} alt={p.name}/>:<div className="fallback">NYZ</div>}</div><div className="cardBody"><span className="tag">{p.category}</span><h3>{p.name}</h3>{p.series&&<div className="muted" style={{fontSize:12,marginBottom:8}}>{p.series}</div>}<div className="row"><span className="price">{rupiah(min)}</span><div className="chips"><Link className="btn" href={`/products/${p.slug}`}>Detail</Link><CartButton p={p} /><WishlistButton productId={p.id} /></div></div></div></article>}
