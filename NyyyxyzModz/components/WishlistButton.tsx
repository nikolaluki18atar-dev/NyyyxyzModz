'use client';import {useState} from 'react';export default function WishlistButton({productId}:{productId:string}){const [x,setX]=useState(false);async function go(){const r=await fetch('/api/wishlist',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({productId})});if(r.ok){const j=await r.json();setX(j.added)}}return <button className="btn" onClick={go} title="Wishlist">{x?'♥':'♡'}</button>}
