'use client';import {useState} from 'react';export default function CartButton({p}:any){const [ok,setOk]=useState(false);function add(){const v=p.variants[0];let a:any[]=[];try{a=JSON.parse(localStorage.getItem('nyz-cart')||'[]')}catch{};const i=a.findIndex(x=>x.id===v.id);if(i>=0)a[i].qty++;else a.push({id:v.id,productSlug:p.slug,name:p.name,variant:v.name,price:v.price,qty:1});localStorage.setItem('nyz-cart',JSON.stringify(a));setOk(true);setTimeout(()=>setOk(false),1200)}return <button className="btn primary" onClick={add}>{ok?'✓ Ditambah':'+ Keranjang'}</button>}
