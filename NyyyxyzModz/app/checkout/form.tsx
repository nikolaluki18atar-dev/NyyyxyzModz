'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';import {rupiah} from '@/lib/utils';
export default function CheckoutForm({product,variant,user}:{product:any;variant:any;user:any}){const [wa,setWa]=useState(user.whatsapp||''),[voucher,setVoucher]=useState(''),[err,setErr]=useState(''),[loading,setLoading]=useState(false);const r=useRouter();async function submit(e:any){e.preventDefault();setLoading(true);setErr('');const x=await fetch('/api/orders',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({items:[{productSlug:product.slug,variantName:variant.name,quantity:1}],whatsapp:wa,voucherCode:voucher||undefined})});const j=await x.json();if(!x.ok)setErr(j.error||'Gagal membuat order');else r.push('/orders/'+j.invoice);setLoading(false)}return <form className="panel" onSubmit={submit}><div className="row"><div><span className="tag">{product.category}</span><h2>{product.name}</h2><p className="muted">Variant: {variant.name}</p></div><strong className="price">{rupiah(variant.price)}</strong></div><div className="field"><label>WhatsApp</label><input required value={wa} onChange={e=>setWa(e.target.value)} placeholder="08xxxxxxxxxx"/></div><div className="field"><label>Voucher</label><input value={voucher} onChange={e=>setVoucher(e.target.value)} placeholder="Opsional"/></div>{user.role==='RESELLER'&&<div className="notice">Credit reseller: <b>{rupiah(user.credit)}</b>. Jika credit mencukupi, order akan diproses otomatis dari credit.</div>}<div className="notice"><b>Manual QRIS</b><br/>Invoice dibuat dulu. Pembayaran customer belum dianggap berhasil sampai admin memverifikasi bukti.</div>{err&&<p className="danger">{err}</p>}<button className="btn primary" disabled={loading}>{loading?'Membuat invoice…':'Buat Invoice'}</button></form>}
