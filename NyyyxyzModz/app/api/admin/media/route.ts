import {NextResponse}from'next/server';import{requireAdmin}from'@/lib/auth';import{db}from'@/lib/db';export async function POST(req:Request){await requireAdmin();const f=await req.formData();const id=String(f.get('id')||'');const action=String(f.get('action')||'');if(id&&action==='delete'){await db.media.delete({where:{id}});return NextResponse.redirect(new URL('/admin/media',req.url))}await db.media.create({data:{name:String(f.get('name')||''),url:String(f.get('url')||''),type:String(f.get('type')||'image')}});return NextResponse.redirect(new URL('/admin/media',req.url))}
