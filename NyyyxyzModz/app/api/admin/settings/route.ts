import {NextResponse}from'next/server';import{requireAdmin}from'@/lib/auth';import{db}from'@/lib/db';import{logActivity}from'@/lib/admin';export async function POST(req:Request){const a=await requireAdmin();const f=await req.formData();const keys=['siteName','siteUrl','whatsapp1','whatsapp2','qris','maintenance','storeDescription','instagram','tiktok','telegram','footerText'];for(const key of keys){if(f.has(key))await db.siteSetting.upsert({where:{key},update:{value:String(f.get(key)||'')},create:{key,value:String(f.get(key)||'')}})}await logActivity('SETTINGS',`Settings diperbarui oleh ${a.email}`,a.id);return NextResponse.redirect(new URL('/admin/settings',req.url))}
