import {NextResponse} from 'next/server';import {requireAdmin} from '@/lib/auth';import {db} from '@/lib/db';export async function POST(req:Request){await requireAdmin();const f=await req.formData();const id=String(f.get('id')||''),status=String(f.get('status')||'REQUESTED');if(id)await db.refund.update({where:{id},data:{status}});return NextResponse.redirect(new URL('/admin/orders',req.url))}
