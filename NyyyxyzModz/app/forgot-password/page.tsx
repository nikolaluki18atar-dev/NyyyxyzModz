'use client';import {useState} from 'react';export default function Forgot(){const [email,setEmail]=useState(''),[msg,setMsg]=useState('');async function go(e:any){e.preventDefault();const r=await fetch('/api/auth/forgot-password',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email})});const j=await r.json();setMsg(j.message||j.error)}return <main className="container section"><form className="panel form" onSubmit={go}><div className="eyebrow">ACCOUNT</div><h1>Lupa Password</h1><div className="field"><label>Email</label><input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></div>{msg&&<p className="muted">{msg}</p>}<button className="btn primary">Kirim Instruksi</button></form></main>}
