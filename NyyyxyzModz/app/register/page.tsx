'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation'; import Link from 'next/link';
export default function Register(){const [f,setF]=useState({name:'',email:'',password:'',confirm:'',whatsapp:''}),[err,setErr]=useState(''),[loading,setLoading]=useState(false);const r=useRouter();const set=(k:string,v:string)=>setF(x=>({...x,[k]:v}));async function submit(e:any){e.preventDefault();setErr('');if(f.password!==f.confirm)return setErr('Password dan konfirmasi tidak sama');setLoading(true);const x=await fetch('/api/auth/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(f)});const j=await x.json();if(!x.ok)setErr(j.error||'Register gagal');else r.push('/account');setLoading(false)}return <main className="container"><form className="form panel" onSubmit={submit}><div className="eyebrow">ACCOUNT</div><h1>Register</h1>{[['name','Nama'],['email','Email'],['whatsapp','WhatsApp'],['password','Password'],['confirm','Confirm Password']].map(([k,l])=><div className="field" key={k}><label>{l}</label><input required type={k==='email'?'email':k.includes('password')||k==='confirm'?'password':'text'} value={(f as any)[k]} onChange={e=>set(k,e.target.value)}/></div>)}{err&&<p className="danger">{err}</p>}<button className="btn primary" disabled={loading}>{loading?'Membuat akun…':'Buat Akun'}</button><p className="muted">Sudah punya akun? <Link href="/login">Login</Link></p></form></main>}
