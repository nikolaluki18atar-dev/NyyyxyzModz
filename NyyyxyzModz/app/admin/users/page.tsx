import {db} from '@/lib/db';
export const dynamic='force-dynamic';
export default async function Users(){const users=await db.user.findMany({orderBy:{createdAt:'desc'},take:200}).catch(()=>[]);return <section><div className="pageTop"><div><div className="eyebrow">PEOPLE</div><h1>Users & Reseller</h1></div><a className="btn" href="/api/admin/export?type=users">Export CSV</a></div><div className="panel"><form className="formGrid" action="/api/admin/users" method="post"><input name="email" type="email" placeholder="Email user/reseller" required/><select name="role"><option>CUSTOMER</option><option>RESELLER</option><option>ADMIN</option></select><input name="credit" type="number" min="0" placeholder="Set credit" defaultValue="0"/><input name="delta" type="number" placeholder="Tambah/kurang credit (contoh 50000 / -10000)"/><button className="btn primary">Update User</button></form><p className="muted small">Untuk keamanan, perubahan role/credit hanya bekerja pada user yang sudah terdaftar.</p></div><div className="panel tableWrap"><table className="table"><thead><tr><th>Nama</th><th>Email</th><th>Role</th><th>Credit</th><th>Status</th><th>Aksi</th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td>{u.name}</td><td>{u.email}</td><td>{u.role}</td><td>Rp {u.credit.toLocaleString('id-ID')}</td><td>{u.active?'Aktif':'Nonaktif'}</td><td><form action="/api/admin/users" method="post"><input type="hidden" name="id" value={u.id}/><input type="hidden" name="action" value="toggle"/><button className="btn smallBtn">Toggle</button></form></td></tr>)}</tbody></table></div></section>}
