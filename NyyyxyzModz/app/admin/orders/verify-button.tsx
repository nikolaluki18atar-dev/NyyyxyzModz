'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation';
export default function VerifyButton({id}:{id:string}){const [busy,setBusy]=useState(false);const r=useRouter();async function go(){if(!confirm('Tandai pembayaran sebagai PAID dan kirim key jika tersedia?'))return;setBusy(true);const x=await fetch('/api/admin/orders/'+id+'/verify',{method:'POST'});const j=await x.json();if(!x.ok)alert(j.error||'Gagal');else alert(j.message);r.refresh();setBusy(false)}return <button className="btn primary" disabled={busy} onClick={go}>{busy?'…':'Verify PAID'}</button>}
