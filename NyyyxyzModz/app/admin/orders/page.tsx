import {db} from '@/lib/db';export const dynamic='force-dynamic';
export default async function Orders(){await db.order.updateMany({where:{paymentStatus:'PENDING',orderStatus:'PENDING',expiresAt:{lt:new Date()}},data:{orderStatus:'CANCELLED'}});const rows=await db.order.findMany({include:{user:true,key:true,items:{include:{keys:true}},refund:true},orderBy:{createdAt:'desc'},take:300});return <section><div className="pageTop"><div><div className="eyebrow">SALES</div><h1>Orders</h1><p className="muted">Verifikasi pembayaran manual dan kelola status order.</p></div><a className="btn" href="/api/admin/export?type=orders">Export CSV</a></div><div className="panel tableWrap"><table className="table"><thead><tr><th>Invoice</th><th>User</th><th>Produk</th><th>Total</th><th>Payment</th><th>Order</th><th>Key</th><th>Refund</th><th>Aksi</th></tr></thead><tbody>{rows.map(o=><tr key={o.id}><td>{o.invoice}</td><td>{o.user.email}</td><td>{o.productName}<br/><span className="muted small">{o.variantName}</span></td><td>Rp {o.total.toLocaleString('id-ID')}</td><td>{o.paymentStatus}</td><td>{o.orderStatus}</td><td>{o.items.flatMap((i:any)=>i.keys.map((k:any)=>k.key)).join(', ')||'-'}</td><td>{o.refund?.status||'-'}{o.refund&&<form action="/api/admin/refunds" method="post"><input type="hidden" name="id" value={o.refund.id}/><select name="status" defaultValue={o.refund.status}><option>REQUESTED</option><option>APPROVED</option><option>PAID</option><option>REJECTED</option></select><button className="btn smallBtn">Update</button></form>}</td><td><div className="chips"><form action={`/api/admin/orders/${o.id}/verify`} method="post"><button className="btn smallBtn" disabled={o.paymentStatus==='PAID'}>Verify</button></form><form action={`/api/admin/orders/${o.id}`} method="post"><input type="hidden" name="action" value="process"/><button className="btn smallBtn">Process</button></form><form action={`/api/admin/orders/${o.id}`} method="post"><input type="hidden" name="action" value="cancel"/><button className="btn smallBtn dangerBtn">Cancel</button></form></div></td></tr>)}</tbody></table></div></section>}
