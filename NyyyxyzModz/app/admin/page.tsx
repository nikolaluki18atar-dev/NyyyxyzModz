import {db} from '@/lib/db';
export const dynamic='force-dynamic';
export default async function Admin(){const [products,orders,users,stock,paid,resellers,credit,vouchers,activities]=await Promise.all([db.product.count().catch(()=>0),db.order.count().catch(()=>0),db.user.count().catch(()=>0),db.productKey.count({where:{status:'AVAILABLE'}}).catch(()=>0),db.order.count({where:{paymentStatus:'PAID'}}).catch(()=>0),db.user.count({where:{role:'RESELLER'}}).catch(()=>0),db.user.aggregate({where:{role:'RESELLER'},_sum:{credit:true}}).catch(()=>({_sum:{credit:0}})),db.voucher.count({where:{active:true}}).catch(()=>0),db.activity.findMany({orderBy:{createdAt:'desc'},take:8}).catch(()=>[])]);return <section><div className="pageTop"><div><div className="eyebrow">CONTROL CENTER</div><h1>Dashboard</h1><p className="muted">Kelola toko, stok, reseller, pembayaran, dan konten dari satu tempat.</p></div></div><div className="adminStats"><div className="stat"><b>{products}</b><span>Products</span></div><div className="stat"><b>{orders}</b><span>Orders</span></div><div className="stat"><b>{users}</b><span>Users</span></div><div className="stat"><b>{stock}</b><span>Available Keys</span></div><div className="stat"><b>{paid}</b><span>Paid Orders</span></div><div className="stat"><b>{resellers}</b><span>Resellers</span></div><div className="stat"><b>Rp {(credit._sum.credit||0).toLocaleString('id-ID')}</b><span>Total Credit</span></div><div className="stat"><b>{vouchers}</b><span>Active Vouchers</span></div></div><div className="panel"><h2>Activity</h2>{activities.length?<div>{activities.map(a=><div className="activity" key={a.id}><span className="dot"/><div><b>{a.message}</b><div className="muted small">{new Date(a.createdAt).toLocaleString('id-ID')}</div></div></div>)}</div>:<p className="muted">Belum ada activity.</p>}</div></section>}
