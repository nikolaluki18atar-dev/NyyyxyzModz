import {db} from '@/lib/db';
export const dynamic='force-dynamic';
export default async function Activity(){const rows=await db.activity.findMany({orderBy:{createdAt:'desc'},take:200,include:{user:true}}).catch(()=>[]);return <section><div className="pageTop"><div><div className="eyebrow">AUDIT LOG</div><h1>Activity</h1></div></div><div className="panel"><div className="activity"><span className="dot"/><div><b>Live activity</b><p className="muted small">Halaman ini membaca activity terbaru dari database. Refresh untuk mengambil data terbaru.</p></div></div>{rows.map(a=><div className="activity" key={a.id}><span className="dot"/><div><b>{a.message}</b><div className="muted small">{a.type} · {a.demo?'DEMO':''} · {new Date(a.createdAt).toLocaleString('id-ID')}</div></div></div>)}</div></section>}
