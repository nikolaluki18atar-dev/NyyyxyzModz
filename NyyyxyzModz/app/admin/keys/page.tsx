import {db} from '@/lib/db';
export const dynamic='force-dynamic';
export default async function Keys(){const variants=await db.productVariant.findMany({where:{active:true},include:{product:true},orderBy:{product:{name:'asc'}}}).catch(()=>[]);const stock=await db.productKey.groupBy({by:['variantId','status'],_count:{_all:true}}).catch(()=>[]);const count=(id:string)=>stock.find(x=>x.variantId===id&&x.status==='AVAILABLE')?._count._all||0;return <section><div className="pageTop"><div><div className="eyebrow">INVENTORY</div><h1>Key Restock</h1><p className="muted">Satu key per baris. Duplikat otomatis ditolak database.</p></div></div><div className="panel"><form className="formGrid" action="/api/admin/keys" method="post"><select name="variantId" required><option value="">Pilih variant</option>{variants.map(v=><option value={v.id} key={v.id}>{v.product.name} — {v.name}</option>)}</select><textarea name="keys" rows={8} placeholder={'KEY-001\nKEY-002\nKEY-003'} required/><button className="btn primary">Restock Keys</button></form></div><div className="panel"><h2>Import / Export</h2><form className="formGrid" action="/api/admin/keys/import" method="post" encType="multipart/form-data"><select name="variantId" required><option value="">Pilih variant</option>{variants.map(v=><option value={v.id} key={v.id}>{v.product.name} — {v.name}</option>)}</select><input type="file" name="file" accept=".txt,.csv" required/><button className="btn primary">Import TXT/CSV</button></form><div className="chips"><a className="btn" href="/api/admin/export?type=keys">Export Semua Key CSV</a></div></div><div className="panel tableWrap"><table className="table"><thead><tr><th>Produk</th><th>Variant</th><th>Harga</th><th>Available</th></tr></thead><tbody>{variants.map(v=><tr key={v.id}><td>{v.product.name}</td><td>{v.name}</td><td>Rp {v.price.toLocaleString('id-ID')}</td><td className="success">{count(v.id)}</td></tr>)}</tbody></table></div></section>}
