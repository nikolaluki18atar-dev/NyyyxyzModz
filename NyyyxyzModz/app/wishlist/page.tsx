import {redirect} from 'next/navigation';import {getSession} from '@/lib/auth';import {db} from '@/lib/db';import Link from 'next/link';export const dynamic='force-dynamic';export default async function Wishlist(){const u=await getSession();if(!u)redirect('/login');const rows=await db.wishlist.findMany({where:{userId:u.id},include:{product:true}});return <main className="container section"><div className="eyebrow">SAVED</div><h1>Wishlist</h1>{!rows.length?<div className="panel"><p className="muted">Belum ada produk tersimpan.</p><Link className="btn primary" href="/products">Lihat produk</Link></div>:<div className="grid">{rows.map(x=><div className="card" key={x.id}><div className="thumb">{x.product.image?<img src={x.product.image} alt={x.product.name}/>:<div className="fallback">NYZ</div>}</div><div className="cardBody"><h3>{x.product.name}</h3><Link className="btn primary full" href={`/products/${x.product.slug}`}>Buka Produk</Link></div></div>)}</div>}</main>}
