'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation'; import Link from 'next/link';
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[err,setErr]=useState(''),[loading,setLoading]=useState(false);const r=useRouter();async function submit(e:any){e.preventDefault();setLoading(true);setErr('');const x=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});const j=await x.json();if(!x.ok)setErr(j.error||'Login gagal');else r.push(j.role==='ADMIN'?'/admin':'/account');setLoading(false)}return <main className="container"><form className="form panel" onSubmit={submit}><div className="eyebrow">ACCOUNT</div><h1>Login</h1><p className="muted">Masuk untuk checkout dan melihat invoice.</p><div className="field"><label>Email</label><input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></div><div className="field"><label>Password</label><input type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></div>{err&&<p className="danger">{err}</p>}<button className="btn primary" disabled={loading}>{loading?'Memproses…':'Login'}</button><p className="muted">Belum punya akun? <Link href="/register">Register</Link></p></form><p className="muted"><a href="/forgot-password">Lupa password?</a></p></main>}
